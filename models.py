
#db.execute("CREATE TABLE lights (Id SERIAL PRIMARY KEY, status DECIMAL NOT NULL, pop DECIMAL NOT NULL)")


#db.execute("COPY lights from "data/sloc.csv"")
#db.execute("SELECT * FROM lights")

#db.commit()
